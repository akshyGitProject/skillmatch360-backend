server:
  port: 9092

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/skillmatch360_db
    username: root
    password: Prisha@2024
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

  application:
    name: skillmatch360-backend

logging:
  level:
    org.springframework: INFO
    com.skillmatch360: DEBUG
